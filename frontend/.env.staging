# Staging Environment Configuration
# Used when running: npm run build:staging

# Backend API URL (Update with your actual staging URL)
VITE_API_URL=https://api-staging.learnmist.com

# WebSocket URL for real-time features (Update with your actual staging URL)
VITE_WS_URL=wss://api-staging.learnmist.com
