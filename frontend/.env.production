# Production Environment Configuration
# Used when running: npm run build:prod or npm run build

# Backend API URL - use "/" so requests go to the same host (34.173.65.197:8000)
VITE_API_URL=/

# WebSocket URL - use relative ws path for the same host
VITE_WS_URL=ws://34.173.65.197:8000
