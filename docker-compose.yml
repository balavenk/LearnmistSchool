version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:8080"
      - "8000:8080"
    environment:
      - PORT=8080
      # Database is internal SQLite, matching Dockerfile default
      - DATABASE_URL=sqlite:///./learnmistschool.db
    restart: always
